import { IframepipePipe } from './iframepipe.pipe';

describe('IframepipePipe', () => {
  it('create an instance', () => {
    const pipe = new IframepipePipe();
    expect(pipe).toBeTruthy();
  });
});
